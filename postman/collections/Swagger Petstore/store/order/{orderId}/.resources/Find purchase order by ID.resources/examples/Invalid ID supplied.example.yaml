$kind: http-example
request:
  url: "{{baseUrl}}/store/order/:orderId"
  method: GET
  pathVariables:
    - key: orderId
      value: "4"
      description: ID of pet that needs to be fetched
response:
  statusCode: 400
  statusText: Bad Request
  body:
    type: text
    content: ""
order: 2000
