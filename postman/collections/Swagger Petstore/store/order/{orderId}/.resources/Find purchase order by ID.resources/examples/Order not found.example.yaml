$kind: http-example
request:
  url: "{{baseUrl}}/store/order/:orderId"
  method: GET
  pathVariables:
    - key: orderId
      value: "4"
      description: ID of pet that needs to be fetched
response:
  statusCode: 404
  statusText: Not Found
  body:
    type: text
    content: ""
order: 3000
